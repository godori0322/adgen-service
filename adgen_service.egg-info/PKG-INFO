Metadata-Version: 2.4
Name: adgen-service
Version: 0.1.0
Summary: 음성 기반 소상공인 마케팅 자동 생성 서비스
Author-email: Team 4 <team4@example.com>
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.121.0
Requires-Dist: uvicorn[standard]>=0.38.0
Requires-Dist: python-multipart>=0.0.20
Requires-Dist: sqlalchemy>=2.0.0
Requires-Dist: passlib[argon2]>=1.7.4
Requires-Dist: python-jose[cryptography]>=3.3.0
Requires-Dist: argon2-cffi>=23.1.0
Requires-Dist: openai>=2.7.1
Requires-Dist: torch>=2.6.0
Requires-Dist: torchaudio>=2.6.0
Requires-Dist: torchvision>=0.21.0
Requires-Dist: diffusers>=0.30.0
Requires-Dist: transformers>=4.44.0
Requires-Dist: accelerate>=0.34.0
Requires-Dist: langchain>=1.0.4
Requires-Dist: numpy>=2.1.2
Requires-Dist: pandas>=2.3.3
Requires-Dist: python-dotenv>=1.2.1
Requires-Dist: requests>=2.31.0
Requires-Dist: tqdm>=4.67.1
Requires-Dist: jupyter>=1.1.1
Requires-Dist: ipykernel>=7.1.0
Requires-Dist: matplotlib>=3.10.7
Requires-Dist: streamlit>=1.51.0
Provides-Extra: dev
Requires-Dist: pytest>=7.4.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.21.0; extra == "dev"
Requires-Dist: httpx>=0.24.0; extra == "dev"
Requires-Dist: black>=23.0.0; extra == "dev"
Requires-Dist: ruff>=0.1.0; extra == "dev"

# Voice2Marketing - 음성 기반 소상공인 마케팅 자동 생성 서비스

소상공인을 위한 AI 광고 생성 서비스

## 🚀 주요 기능

- **음성 입력**: Whisper API를 통한 음성→텍스트 변환
- **상황 맞춤형 마케팅**: GPT-4o-mini를 활용한 업종별, 날씨별 맞춤 아이디어 생성
- **자동 이미지 생성**: Stable Diffusion을 통한 홍보 포스터 자동 생성
- **사용자 인증**: JWT 기반 회원가입/로그인
- **개인화**: 업종, 위치, 메뉴 등 사업자 정보 기반 맞춤형 콘텐츠

## 📦 설치 및 실행

### 빠른 시작 (Quick Start)

```bash
# 1. 저장소 클론
git clone https://github.com/godori0322/adgen-service.git
cd adgen-service/adgen-service

# 2. Python 가상환경 생성 및 활성화
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 3. 의존성 설치
pip install -e .
# 또는
pip install -e ".[dev]"  # 개발 도구 포함

# 4. 환경변수 설정
cp .env.example .env
# .env 파일을 열어서 API 키들을 입력하세요

# 5. 데이터베이스 초기화 (선택사항)
python backend/init_db.py

# 6. 서버 실행
uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000
```

서버가 실행되면:
- API: http://localhost:8000
- API 문서: http://localhost:8000/docs
- 대체 문서: http://localhost:8000/redoc

### 환경변수 설정 (.env 파일)

```.env
# OpenAI API Key (필수)
OPENAI_API_KEY=your_openai_api_key_here

# Hugging Face API Key (필수)
HF_API_KEY=your_huggingface_api_key_here

# Weather API Key (필수 - OpenWeatherMap)
WEATHER_API_KEY=your_weather_api_key_here

# Database URL (기본값: SQLite)
DATABASE_URL=sqlite:///./adgen.db

# JWT Secret Key (프로덕션에서는 반드시 변경!)
SECRET_KEY=your-super-secret-key-change-this-in-production
```

### 의존성만 설치하기

```bash
# 기본 패키지만
pip install fastapi uvicorn sqlalchemy passlib python-jose bcrypt python-dotenv openai requests python-multipart

# 또는 requirements.txt 생성해서 사용
pip freeze > requirements.txt
```

## 📚 API 문서

서버 실행 후 http://localhost:8000/docs 에서 Swagger UI를 통해 API 문서를 확인할 수 있습니다.

## 🔑 주요 엔드포인트

### 인증
- `POST /api/auth/register` - 회원가입
- `POST /api/auth/login` - 로그인
- `GET /api/auth/me` - 내 정보 조회
- `PUT /api/auth/me` - 내 정보 수정

### 광고 생성
- `POST /api/ads/generate` - 음성 기반 광고 생성 (인증 필요)

### 개별 서비스
- `POST /api/whisper/transcribe` - 음성→텍스트 변환
- `POST /api/gpt/generate` - 마케팅 아이디어 생성
- `POST /api/diffusion/generate` - 이미지 생성
- `GET /api/weather` - 날씨 정보 조회

## 🏗️ 프로젝트 구조

```
backend/
├── app/
│   ├── main.py              # FastAPI 앱 진입점
│   ├── api/
│   │   ├── router.py        # API 라우터 통합
│   │   └── routes/          # 엔드포인트별 라우트
│   │       ├── auth.py      # 인증 라우트
│   │       ├── ads.py       # 광고 생성 라우트
│   │       ├── whisper.py
│   │       ├── gpt.py
│   │       ├── diffusion.py
│   │       └── weather.py
│   ├── services/            # 비즈니스 로직
│   │   ├── auth_service.py
│   │   ├── whisper_service.py
│   │   ├── gpt_service.py
│   │   ├── diffusion_service.py
│   │   └── weather_service.py
│   └── core/
│       ├── database.py      # DB 설정
│       ├── models.py        # SQLAlchemy 모델
│       └── schemas.py       # Pydantic 스키마
└── init_db.py              # DB 초기화 스크립트
```

## 💡 사용 예시

### 1. 회원가입
```bash
curl -X POST "http://localhost:8000/api/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "cafe_owner",
    "email": "owner@example.com",
    "password": "password123",
    "business_type": "카페",
    "location": "서울 강남구",
    "menu_items": ["아메리카노", "라떼", "케이크"],
    "business_hours": "09:00-22:00"
  }'
```

### 2. 로그인
```bash
curl -X POST "http://localhost:8000/api/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=cafe_owner&password=password123"
```

### 3. 광고 생성 (음성 파일 필요)
```bash
curl -X POST "http://localhost:8000/api/ads/generate" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -F "file=@voice.mp3" \
  -F "city=Seoul"
```

## 🛠️ 기술 스택

- **Backend**: FastAPI
- **Database**: SQLAlchemy + SQLite (PostgreSQL로 변경 가능)
- **Authentication**: JWT (python-jose)
- **Password Hashing**: Passlib (bcrypt)
- **AI Services**: 
  - OpenAI Whisper (음성 인식)
  - GPT-4o-mini (마케팅 생성)
  - Stable Diffusion (이미지 생성)
- **Weather API**: OpenWeatherMap
